#include<stdio.h>
int main(){
    
    int* aptr;
    char *bptr;
    float *fptr;


    printf("size of aptr is: %u\n",sizeof(aptr));
    printf("size of bptr is: %u\n",sizeof(bptr));
    printf("size of fptr is: %u\n",sizeof(fptr));
    return 0;

}